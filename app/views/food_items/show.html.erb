<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @food_item.name %>
</p>

<p>
  <strong>Expiry date:</strong>
  <%= @food_item.expiry_date%> days
</p>
<p>
  <strong>Food item image:</strong>
  <%= link_to image_tag(@food_item.food_item_image.thumb.url), @food_item.food_item_image.detail.url%>
</p>
<p>
  <strong>User:</strong>
  <%= current_user.try(:first_name) %>
<!-- @food_item.user_id -->
</p>



<p>
  <strong>Ingredient:</strong>
  <%= @food_item.ingredient.name%>
</p>

<%= link_to 'Edit', edit_food_item_path(@food_item) %> |
<td> <%= link_to 'Delete', @food_item, method: :delete, data: { confirm: 'Are you sure?' }%></h3></td>
<%= link_to 'Back', food_items_path %>
